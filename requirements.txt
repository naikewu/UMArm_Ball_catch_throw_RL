# Workspace Python dependencies.
#
# The workspace venv (.venv) is seeded from the base interpreter
#   C:\Users\zuorunze\AppData\Local\Programs\Python\Python313\python.exe
# with --system-site-packages. That base 3.13 install already carries every
# entry below EXCEPT python-can, so a fresh .venv needs exactly one PyPI
# download. The lines are kept here anyway so the set is reproducible on a
# machine that does not have the base install.

mujoco>=3.11
numpy
scipy
matplotlib
pyserial
python-can>=4.3
pytest
# digital_twin.mech_fit's optimiser (CMA-ES); installed into .venv 2026-09-10 (4.4.4)
cma>=4.0

# Dynamic-controller training/inference and shareable simulation videos.
torch>=2.6
threadpoolctl>=3.6
opencv-python
imageio-ffmpeg>=0.6
