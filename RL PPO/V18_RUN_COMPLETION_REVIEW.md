# V18完成检查与入口补强

## 进程与训练

2026-09-19再次检查时，系统没有运行中的Python训练/评估进程。
`explore_throw/training_summary.json`确认10轮、160条在线训练已完成，耗时611.33 s（约10分11秒）。
并非对话中断导致训练停在半途。10个更新文件、3个验证文件、latest和candidate检查点完整。

关键区别：`ppo_latest.pt`是第10轮；`ppo_candidate.pt`仍是第0轮零残差基线；没有`ppo_accepted.pt`。
后两轮验证没有超过研究候选的排序条件，因此保留第0轮是正常选模结果，不是没有进行梯度更新。

## 训练与选模验证结果

随机采样160条：134条抓住并释放，122条在15 cm内命中，0条触发安全中止。
随机训练中实际发生9次策略主动释放、125次预测器释放，表明策略释放接口参与了探索。
第1-10轮近似KL约0.00070-0.00152，没有KL提前停止。

| 固定20条选模验证 | 原BC | V18零残差 | 第5轮 | 第10轮 |
|---|---:|---:|---:|---:|
| 抓住/释放 | 16 | 16 | 16 | 16 |
| 15 cm内命中 | 16 | 15 | 15 | 15 |
| 平均落点误差 | 4.53 cm | 7.32 cm | 7.47 cm | 7.94 cm |
| 平均接球到释放 | 未列出 | 10.93 s | 8.07 s | 7.68 s |
| 最大关节偏转 | 未列出 | 40.66度 | 40.57度 | 40.52度 |

第10轮相对零残差的初始峰值、冲量和接球相对速度没有改善。默认throw阶段未开放接球动作，这是预期的控制权限限制。
确定性验证中三版V18均由预测器释放，没有策略主动释放；不能将随机训练的主动释放记录当作已学会更好释放时机。
接球后平均10.35 ms开始轨道控制，检测到的最长低速停顿28 ms。并不存在原来的固定停稳等待，但释放耗时、精度及关节偏转仍需权衡。

当前证据是“甩动耗时缩短，但成功率/精度和柔顺性没有胜过BC”，不是训练全面进步。

## 本次脚本修改

- `START_V18_RL.ps1 -Mode Status`：只读检查轮次、文件完整性、选模一致性、源码/BC指纹以及评估状态。
- `-Mode Evaluate -Checkpoint Latest`：明确评估最新训练模型，不再只能通过启动器评估candidate。
- 支持Candidate/Latest/Accepted，分别使用评估目录，避免相互覆盖。缺少所选检查点时提前报错。
- Candidate为第0轮时给出警告，避免把零残差基线称为训练后的RL。
- 新增12项状态测试，包括缺失文件、初始化/恢复、选模中断、来源不一致和评估完成标记；连同原回归测试共87 passed，52.50 s。

本次未改变控制模型、网络、训练奖励或训练源指纹，未重训，也未覆盖160条训练产物。
V18指纹仍为 `f30cb3dcce30c897493ba0611e9ce914e7d2357d3d802939f64a03a7be6b1cf9`。
状态检查不等同于操作系统进程检测，检查点摘要也不等同于性能验收。

## 命令

```powershell
& '.\RL PPO\START_V18_RL.ps1' -Mode Status
& '.\RL PPO\START_V18_RL.ps1' -Mode Evaluate -Checkpoint Latest -Episodes 40
```

Latest结果输出在 `teacher_runs/v18_online/explore_throw/evaluation_latest_40/`。
评估器内部历史字段`candidate_episodes.json`表示本次所选模型，身份以`evaluation_contract.json`中的`selected_update`与检查点哈希为准；本次应为第10轮。
已有训练完成，不要再次运行Explore到同一个目录；Resume也不会追加训练轮数。
